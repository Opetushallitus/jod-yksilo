logging:
  level:
    root: INFO
    fi.okm.jod: DEBUG
spring:
  sql:
    init:
      mode: never
  cloud:
    aws:
      parameterstore:
        enabled: false
jod:
  integraatio:
    koski:
      hosts:
        - "testiopintopolku.fi"
