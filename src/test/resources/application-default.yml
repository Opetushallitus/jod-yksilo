logging:
  level:
    root: INFO
    fi.okm.jod: DEBUG
  exception-conversion-word:

spring:
  main:
    lazy-initialization: true
  datasource:
    hikari:
      maximum-pool-size: 3
      schema: yksilo

server:
  shutdown: immediate

management:
  defaults:
    metrics:
      export:
        enabled: false

jod:
  external-api:
    apiKey: "9g43fg49873gr97gtd3d343f7g3497gf"
  integraatio:
    koski:
      hosts:
        - "testiopintopolku.fi"
