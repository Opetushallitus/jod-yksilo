
logging:
  level:
    root: INFO
    fi.okm.jod: DEBUG
  exception-conversion-word:

spring:
  sql:
    init:
      mode: never
  cloud:
    aws:
      parameterstore:
        enabled: false

management:
  simple:
    metrics:
      export:
        enabled: false
jod:
  integraatio:
    koski:
      hosts:
        - "testiopintopolku.fi"
