logging:
  level:
    root: INFO
    fi.okm.jod: DEBUG
  exception-conversion-word:

spring:
  cloud:
    aws:
      sqs:
        enabled: false
  main:
    lazy-initialization: true
  datasource:
    hikari:
      maximum-pool-size: 3
      schema: yksilo

server:
  shutdown: immediate

management:
  defaults:
    metrics:
      export:
        enabled: false

jod:
  cv:
    request-queue: http://localhost:8430
  external-api:
    apiKey: "dummy-api-key"
  integraatio:
    koski:
      hosts:
        - "testiopintopolku.fi"
