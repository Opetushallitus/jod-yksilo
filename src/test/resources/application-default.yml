logging:
  level:
    root: INFO
    fi.okm.jod: DEBUG
  exception-conversion-word:

spring:
  #main:
  #  lazy-initialization: true
  test:
    context:
      cache:
        maxSize: 16
  datasource:
    hikari:
      maximum-pool-size: 3
      schema: yksilo

server:
  shutdown: immediate

management:
  simple:
    metrics:
      export:
        enabled: false
jod:
  integraatio:
    koski:
      hosts:
        - "testiopintopolku.fi"
